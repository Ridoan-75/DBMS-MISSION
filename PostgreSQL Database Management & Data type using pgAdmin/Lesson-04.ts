=========================================================================================
                    CREATE & DROP DATABASE / TABLE 
=========================================================================================

=========================================================================================
# 0. CREATE & DROP মানে কী?
=========================================================================================
CREATE →
👉 নতুন Database বা Table তৈরি করা

DROP →
👉 Database বা Table **পুরোপুরি মুছে ফেলা** (Permanent)

⚠️ DROP করলে Data ফেরত আসে না

=========================================================================================
# 1. DATABASE কী?
=========================================================================================
Database হলো —
👉 সব Table, Data, Schema রাখার Container

Example:
• ecommerce_db
• university_db
• vehicle_rental_db

=========================================================================================
# 2. DATABASE CREATE করা
=========================================================================================
Syntax:

CREATE DATABASE database_name;

-----------------------------------------------------------------------------------------
Example:
-----------------------------------------------------------------------------------------
CREATE DATABASE ecommerce_db;

👉 এখন ecommerce_db নামে নতুন Database তৈরি হলো

=========================================================================================
# 3. DATABASE List দেখা
=========================================================================================
PostgreSQL এ সব Database দেখতে:

SELECT datname FROM pg_database;

=========================================================================================
# 4. DATABASE Select করা
=========================================================================================
GUI Tool (pgAdmin / Beekeeper) এ —
• Database এ Click করলেই Select হয়

CLI (psql) এ —
\c ecommerce_db

=========================================================================================
# 5. DATABASE DROP করা
=========================================================================================
Syntax:

DROP DATABASE database_name;

-----------------------------------------------------------------------------------------
Example:
-----------------------------------------------------------------------------------------
DROP DATABASE ecommerce_db;

⚠️ Warning:
• ecommerce_db
• এর সব Table
• সব Data
সব Permanently Delete হবে

=========================================================================================
# 6. IF EXISTS ব্যবহার করা (Safe Way)
=========================================================================================
Error avoid করার জন্য:

DROP DATABASE IF EXISTS ecommerce_db;

=========================================================================================
# 7. TABLE কী?
=========================================================================================
Table হলো —
👉 Row + Column এর Structure যেখানে আসল Data থাকে

Example Table:
• users
• products
• orders

=========================================================================================
# 8. TABLE CREATE করা
=========================================================================================
Syntax:

CREATE TABLE table_name (
    column_name data_type,
    column_name data_type
);

-----------------------------------------------------------------------------------------
Example:
-----------------------------------------------------------------------------------------
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50),
    email VARCHAR(100),
    age INTEGER,
    is_active BOOLEAN DEFAULT TRUE
);

=========================================================================================
# 9. TABLE Structure বোঝা
=========================================================================================
users Table:

Column      | Data Type
------------|----------------
id          | SERIAL (PK)
name        | VARCHAR
email       | VARCHAR
age         | INTEGER
is_active   | BOOLEAN

=========================================================================================
# 10. TABLE List দেখা
=========================================================================================
Current Database এর সব Table দেখতে:

SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public';

=========================================================================================
# 11. TABLE DROP করা
=========================================================================================
Syntax:

DROP TABLE table_name;

-----------------------------------------------------------------------------------------
Example:
-----------------------------------------------------------------------------------------
DROP TABLE users;

⚠️ users table + সব data delete হয়ে যাবে

=========================================================================================
# 12. DROP TABLE IF EXISTS
=========================================================================================
Safe way:

DROP TABLE IF EXISTS users;

=========================================================================================
# 13. Multiple Table একসাথে DROP করা
=========================================================================================
DROP TABLE users, products, orders;

=========================================================================================
# 14. TABLE Re-create করার Common Pattern
=========================================================================================
DROP TABLE IF EXISTS users;

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50),
    email VARCHAR(100)
);

=========================================================================================
# 15. CREATE TABLE WITH CONSTRAINTS
=========================================================================================
Example:

CREATE TABLE employees (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    age INTEGER CHECK (age >= 18),
    is_active BOOLEAN DEFAULT TRUE
);

=========================================================================================
# 16. DROP DATABASE vs DROP TABLE
=========================================================================================
DROP DATABASE →
• পুরো Database delete
• সব Table, Data শেষ

DROP TABLE →
• শুধু নির্দিষ্ট Table delete
• Database থাকবে

=========================================================================================
# 17. Common Beginner Mistakes
=========================================================================================
❌ Production DB তে DROP DATABASE  
❌ WHERE ছাড়া DELETE আর DROP গুলিয়ে ফেলা  
❌ Active Database না দেখে TABLE CREATE করা  

=========================================================================================
# 18. Best Practices
=========================================================================================
✔ DROP করার আগে Database নাম Check  
✔ IF EXISTS ব্যবহার করা  
✔ Important DB তে DROP না করা  
✔ Development DB তে Practice করা  

=========================================================================================
# 19. Real Project Workflow
=========================================================================================
Typical Flow:

1) CREATE DATABASE  
2) CREATE TABLE  
3) INSERT DATA  
4) TEST PROJECT  
5) DROP TABLE (if needed)  
6) RE-CREATE TABLE  

=========================================================================================
# 20. QUICK REVISION
=========================================================================================
CREATE DATABASE → নতুন DB  
DROP DATABASE   → DB + সব Data delete  

CREATE TABLE    → নতুন Table  
DROP TABLE      → Table + Data delete  

=========================================================================================
                                    THE END
=========================================================================================
