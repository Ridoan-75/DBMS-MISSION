=========================================================================================
                                INTRODUCTION TO SQL
=========================================================================================

=========================================================================================
# 0. SQL কী?
=========================================================================================
SQL এর পূর্ণরূপ হলো **Structured Query Language**  
এটি একটি **Standard Language**, যা ব্যবহার করা হয়—

- Database তৈরি করতে
- Database থেকে Data আনতে
- Database এ Data ঢুকাতে
- Database এর Data পরিবর্তন করতে
- Database এর Data মুছতে
- Database Structure (Table, Column, Constraint) নিয়ন্ত্রণ করতে

সহজ ভাষায়—
👉 **SQL = Database এর সাথে কথা বলার ভাষা**

=========================================================================================
# 1. SQL কোথায় ব্যবহার হয়?
=========================================================================================
SQL ব্যবহার হয় প্রায় সব Relational Database এ:

- MySQL
- PostgreSQL
- Oracle
- SQL Server
- SQLite

⚠️ Syntax প্রায় একই, শুধু কিছু advanced feature এ পার্থক্য থাকে।

=========================================================================================
# 2. Database, Table, Row, Column (Quick Recap)
=========================================================================================


Database  → একটি বড় Container (Folder এর মতো)
Table     → Database এর ভেতরের Structure (Excel Sheet এর মতো)
Row       → একটি সম্পূর্ণ Record
Column    → একটি নির্দিষ্ট Attribute


=========================================================================================
# 3. SQL COMMAND এর CATEGORY
=========================================================================================
SQL Command মূলত 5 ভাগে ভাগ করা হয়:

-----------------------------------------------------------------------------------------
# A. DDL (Data Definition Language)
-----------------------------------------------------------------------------------------
👉 Database Structure তৈরি ও পরিবর্তনের জন্য

- CREATE
- ALTER
- DROP
- TRUNCATE

-----------------------------------------------------------------------------------------
# B. DML (Data Manipulation Language)
-----------------------------------------------------------------------------------------
👉 Table এর Data নিয়ে কাজ করার জন্য

- INSERT
- UPDATE
- DELETE

-----------------------------------------------------------------------------------------
# C. DQL (Data Query Language)
-----------------------------------------------------------------------------------------
👉 Data দেখার জন্য

- SELECT

-----------------------------------------------------------------------------------------
# D. DCL (Data Control Language)
-----------------------------------------------------------------------------------------
👉 Permission & Security এর জন্য

- GRANT
- REVOKE

-----------------------------------------------------------------------------------------
# E. TCL (Transaction Control Language)
-----------------------------------------------------------------------------------------
👉 Transaction handle করার জন্য

- COMMIT
- ROLLBACK
- SAVEPOINT

=========================================================================================
# 4. আমাদের প্রথম SQL QUERY
=========================================================================================

-- একটি Table থেকে সব Data দেখার জন্য
SELECT * FROM users;

-- নির্দিষ্ট Column দেখার জন্য
SELECT name, email FROM users;

=========================================================================================
# 5. SQL এ TABLE তৈরি করা (CREATE TABLE)
=========================================================================================

CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    age INT
);

-- Explanation:
-- id    → Unique Identifier
-- name  → User এর নাম
-- email → User এর ইমেইল
-- age   → User এর বয়স

=========================================================================================
# 6. DATA INSERT করা (INSERT)
=========================================================================================

INSERT INTO users (id, name, email, age)
VALUES (1, 'Ridoan', 'ridoan@gmail.com', 23);

=========================================================================================
# 7. DATA দেখা (SELECT)
=========================================================================================

-- সব Data
SELECT * FROM users;

-- শর্ত দিয়ে Data
SELECT * FROM users WHERE age > 20;

-- নির্দিষ্ট Column
SELECT name, email FROM users;

=========================================================================================
# 8. DATA UPDATE করা (UPDATE)
=========================================================================================

UPDATE users
SET age = 24
WHERE id = 1;

⚠️ WHERE না দিলে সব Row Update হয়ে যাবে (Dangerous)

=========================================================================================
# 9. DATA DELETE করা (DELETE)
=========================================================================================

DELETE FROM users
WHERE id = 1;

⚠️ WHERE না দিলে পুরো Table খালি হয়ে যাবে

=========================================================================================
# 10. WHERE Clause (Filtering Data)
=========================================================================================

SELECT * FROM users WHERE age = 25;
SELECT * FROM users WHERE age > 18;
SELECT * FROM users WHERE age BETWEEN 18 AND 30;
SELECT * FROM users WHERE name = 'Ridoan';

=========================================================================================
# 11. AND / OR / NOT
=========================================================================================

SELECT * FROM users
WHERE age > 20 AND age < 30;

SELECT * FROM users
WHERE name = 'Ridoan' OR name = 'Hasan';

SELECT * FROM users
WHERE NOT age = 25;

=========================================================================================
# 12. ORDER BY (Sorting Data)
=========================================================================================

-- Ascending Order
SELECT * FROM users ORDER BY age ASC;

-- Descending Order
SELECT * FROM users ORDER BY age DESC;

=========================================================================================
# 13. LIMIT (Data সীমাবদ্ধ করা)
=========================================================================================

-- শুধু প্রথম 5 টা Data
SELECT * FROM users LIMIT 5;

=========================================================================================
# 14. SQL কেন Backend Developer এর জন্য গুরুত্বপূর্ণ?
=========================================================================================

Backend এ সবকিছুই Data কেন্দ্রিক:

- User
- Orders
- Payments
- Products
- Roles & Permissions

👉 SQL না জানলে—
- Efficient Query লেখা যাবে না
- Performance খারাপ হবে
- Real-world Project এ সমস্যা হবে

=========================================================================================
# 15. SQL শেখার পর তুমি কী কী পারবে?
=========================================================================================

✔ Database Design বুঝতে  
✔ Efficient Query লিখতে  
✔ Backend API এর Data Handle করতে  
✔ Real-world Project করতে  
✔ Interview Question Handle করতে  

=========================================================================================
# 16. Next Topics (Roadmap)
=========================================================================================

1️⃣ WHERE, LIKE, IN  
2️⃣ Aggregate Functions (COUNT, SUM, AVG)  
3️⃣ GROUP BY & HAVING  
4️⃣ JOINS (INNER, LEFT, RIGHT)  
5️⃣ Subquery  
6️⃣ Index  
7️⃣ Normalization  
8️⃣ PostgreSQL Deep Dive  

=========================================================================================
                                   THE END
=========================================================================================
