=========================================================================================
                         HANDLING NULL WITH COALESCE 
=========================================================================================

=========================================================================================
# 0. NULL কী?
=========================================================================================
NULL মানে হলো —
👉 কোনো মান নেই / অজানা মান / এখনো নির্ধারিত হয়নি

গুরুত্বপূর্ণ কথা:
- NULL ≠ 0
- NULL ≠ ''
- NULL ≠ FALSE

NULL মানে হলো — "আমি জানি না মানটা কী"

=========================================================================================
# 1. COALESCE কী?
=========================================================================================
COALESCE হলো একটি SQL Scalar Function

👉 কাজ:
- একাধিক value এর মধ্যে
- **প্রথম যে value টি NULL না**
- সেটাই return করে

সহজ ভাষায়:
"যেটা NULL না, সেটাই নাও"

=========================================================================================
# 2. COALESCE এর Syntax
=========================================================================================
COALESCE(value1, value2, value3, ...)

নিয়ম:
- বাম দিক থেকে চেক শুরু করে
- যেই value প্রথমে NULL না হবে
- সেটাই return হবে
- সবগুলো NULL হলে → NULL return করবে

=========================================================================================
# 3. Basic Example (Single Column)
=========================================================================================
ধরা যাক একটি table আছে:

TABLE: users

id | name   | phone
---|--------|---------
1  | Rafi   | 017xxxx
2  | Sumi   | NULL
3  | Karim  | 018xxxx

Query:
-----------------------------------------------------------------------------------------
SELECT
    name,
    COALESCE(phone, 'Phone Not Provided') AS phone_number
FROM users;
-----------------------------------------------------------------------------------------

Result:
-----------------------------------------------------------------------------------------
name   | phone_number
-------|---------------------
Rafi   | 017xxxx
Sumi   | Phone Not Provided
Karim  | 018xxxx
-----------------------------------------------------------------------------------------

ব্যাখ্যা:
- Sumi এর phone NULL
- তাই default text দেখানো হয়েছে

=========================================================================================
# 4. Multiple Values এর সাথে COALESCE
=========================================================================================
COALESCE একাধিক value নিতে পারে

Example:
-----------------------------------------------------------------------------------------
SELECT
    COALESCE(NULL, NULL, 'A', 'B');
-----------------------------------------------------------------------------------------

Result:
-----------------------------------------------------------------------------------------
A
-----------------------------------------------------------------------------------------

কারণ:
- প্রথম NULL ❌
- দ্বিতীয় NULL ❌
- 'A' NULL না ✔
- তাই 'A' return

=========================================================================================
# 5. Column + Default Value
=========================================================================================
ধরা যাক salary column আছে:

id | name   | salary
---|--------|---------
1  | Rafi   | 50000
2  | Sumi   | NULL
3  | Karim  | 40000

Query:
-----------------------------------------------------------------------------------------
SELECT
    name,
    COALESCE(salary, 0) AS salary_amount
FROM employees;
-----------------------------------------------------------------------------------------

Result:
-----------------------------------------------------------------------------------------
name   | salary_amount
-------|---------------
Rafi   | 50000
Sumi   | 0
Karim  | 40000
-----------------------------------------------------------------------------------------

ব্যবহার:
- NULL salary থাকলে 0 দেখাবে

=========================================================================================
# 6. COALESCE With Arithmetic Operations
=========================================================================================
NULL থাকলে calculation ভেঙে যায়

ভুল উদাহরণ:
-----------------------------------------------------------------------------------------
SELECT salary + bonus FROM employees;
-----------------------------------------------------------------------------------------
যদি bonus NULL হয় → পুরো result NULL

সঠিক উপায়:
-----------------------------------------------------------------------------------------
SELECT
    salary + COALESCE(bonus, 0) AS total_income
FROM employees;
-----------------------------------------------------------------------------------------

ফায়দা:
- bonus NULL হলেও calculation ঠিক থাকবে

=========================================================================================
# 7. COALESCE With Aggregate Functions
=========================================================================================
SUM, AVG ইত্যাদির সাথে NULL common সমস্যা

Example:
-----------------------------------------------------------------------------------------
SELECT
    COALESCE(SUM(salary), 0) AS total_salary
FROM employees;
-----------------------------------------------------------------------------------------

কেন দরকার?
- যদি table খালি হয়
- SUM() return করে NULL
- COALESCE দিয়ে 0 বানানো যায়

=========================================================================================
# 8. COALESCE vs IS NULL
=========================================================================================
IS NULL:
-----------------------------------------------------------------------------------------
SELECT * FROM users WHERE phone IS NULL;
-----------------------------------------------------------------------------------------
👉 filtering এর জন্য

COALESCE:
-----------------------------------------------------------------------------------------
SELECT COALESCE(phone, 'N/A') FROM users;
-----------------------------------------------------------------------------------------
👉 display / calculation এর জন্য

দুটার কাজ আলাদা ❗

=========================================================================================
# 9. COALESCE vs IFNULL vs NVL
=========================================================================================
PostgreSQL:
- COALESCE ✔ (Standard SQL)
- IFNULL ❌
- NVL ❌

MySQL:
- COALESCE ✔
- IFNULL ✔

Oracle:
- COALESCE ✔
- NVL ✔

👉 Best Practice:
সব জায়গায় COALESCE ব্যবহার করো

=========================================================================================
# 10. Real-Life Use Cases
=========================================================================================
✔ User profile এ missing data দেখানো  
✔ Salary / price calculation  
✔ Report generation  
✔ NULL safe arithmetic  
✔ Default value show করা  

=========================================================================================
# 11. Common Mistakes
=========================================================================================
❌ NULL = 0 ধরে নেওয়া  
❌ Calculation এ COALESCE না ব্যবহার করা  
❌ Filtering এর জায়গায় COALESCE ব্যবহার করা  

=========================================================================================
# 12. Summary (Final Takeaway)
=========================================================================================
- NULL মানে value নেই
- COALESCE NULL handle করার সবচেয়ে safe উপায়
- First non-NULL value return করে
- Calculation, display, report — সব জায়গায় দরকার
- Standard SQL function

=========================================================================================
                                 THE END
=========================================================================================
