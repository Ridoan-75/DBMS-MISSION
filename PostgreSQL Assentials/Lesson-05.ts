=========================================================================================
                            GROUP BY EXPLAINED
=========================================================================================

=========================================================================================
# 0. GROUP BY কী?
=========================================================================================
GROUP BY হলো একটি SQL clause যেটা ব্যবহার করা হয় —
👉 **একই ধরনের value গুলোকে group (দল) বানানোর জন্য**

সহজ ভাষায়:
- যেসব row এর value একই
- সেগুলোকে একত্রে group করা

👉 GROUP BY সাধারণত Aggregate Function এর সাথে ব্যবহার হয়

=========================================================================================
# 1. কেন GROUP BY দরকার?
=========================================================================================
ধরা যাক sales table:

id | product   | amount
---|-----------|--------
1  | Laptop    | 80000
2  | Mouse     | 800
3  | Laptop    | 75000
4  | Mouse     | 900
5  | Keyboard  | 1500

প্রশ্ন:
👉 প্রতি product এর মোট sales কত?

এই প্রশ্নের উত্তর GROUP BY ছাড়া সম্ভব না ❌

=========================================================================================
# 2. GROUP BY এর Basic Syntax
=========================================================================================
SELECT column_name, AGGREGATE_FUNCTION(column_name)
FROM table_name
GROUP BY column_name;

=========================================================================================
# 3. GROUP BY With COUNT()
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    COUNT(*) AS total_orders
FROM sales
GROUP BY product;
-----------------------------------------------------------------------------------------

Result:
-----------------------------------------------------------------------------------------
product   | total_orders
----------|--------------
Laptop    | 2
Mouse     | 2
Keyboard  | 1
-----------------------------------------------------------------------------------------

ব্যাখ্যা:
- একই product name একত্রে group হয়েছে
- প্রতিটা group এর row সংখ্যা count হয়েছে

=========================================================================================
# 4. GROUP BY With SUM()
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    SUM(amount) AS total_sales
FROM sales
GROUP BY product;
-----------------------------------------------------------------------------------------

Result:
-----------------------------------------------------------------------------------------
product   | total_sales
----------|-------------
Laptop    | 155000
Mouse     | 1700
Keyboard  | 1500
-----------------------------------------------------------------------------------------

=========================================================================================
# 5. GROUP BY With AVG()
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    AVG(amount) AS avg_price
FROM sales
GROUP BY product;
-----------------------------------------------------------------------------------------

ব্যবহার:
- Average salary
- Average marks
- Average order value

=========================================================================================
# 6. GROUP BY With MIN() & MAX()
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    MIN(amount) AS min_price,
    MAX(amount) AS max_price
FROM sales
GROUP BY product;
-----------------------------------------------------------------------------------------

=========================================================================================
# 7. GROUP BY Multiple Columns
=========================================================================================
ধরা যাক table: orders

id | customer | product | amount
---|----------|---------|--------
1  | Rafi     | Laptop  | 80000
2  | Rafi     | Mouse   | 800
3  | Sumi     | Laptop  | 75000
4  | Sumi     | Mouse   | 900

-----------------------------------------------------------------------------------------
SELECT
    customer,
    product,
    SUM(amount) AS total_spent
FROM orders
GROUP BY customer, product;
-----------------------------------------------------------------------------------------

ব্যাখ্যা:
- customer অনুযায়ী group
- তারপর product অনুযায়ী sub-group

=========================================================================================
# 8. GROUP BY With WHERE
=========================================================================================
WHERE আগে কাজ করে, GROUP BY পরে

-----------------------------------------------------------------------------------------
SELECT
    product,
    SUM(amount) AS total_sales
FROM sales
WHERE amount > 1000
GROUP BY product;
-----------------------------------------------------------------------------------------

মানে:
- আগে filter
- তারপর group

=========================================================================================
# 9. HAVING vs WHERE (VERY IMPORTANT ❗)
=========================================================================================
WHERE:
- Row level filter
- GROUP BY এর আগে চলে

HAVING:
- Group level filter
- GROUP BY এর পরে চলে

=========================================================================================
# 10. GROUP BY With HAVING
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    SUM(amount) AS total_sales
FROM sales
GROUP BY product
HAVING SUM(amount) > 2000;
-----------------------------------------------------------------------------------------

ব্যাখ্যা:
- যেসব product এর total sales > 2000
- শুধু সেগুলো দেখাবে

=========================================================================================
# 11. GROUP BY With ORDER BY
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    SUM(amount) AS total_sales
FROM sales
GROUP BY product
ORDER BY total_sales DESC;
-----------------------------------------------------------------------------------------

ব্যবহার:
- Top selling products
- Highest salary departments

=========================================================================================
# 12. GROUP BY With COUNT(DISTINCT)
=========================================================================================
-----------------------------------------------------------------------------------------
SELECT
    product,
    COUNT(DISTINCT customer_id) AS unique_customers
FROM orders
GROUP BY product;
-----------------------------------------------------------------------------------------

মানে:
- প্রতিটা product কতজন আলাদা customer কিনেছে

=========================================================================================
# 13. GROUP BY Rules (EXAM + INTERVIEW 🔥)
=========================================================================================
নিয়ম:
-----------------------------------------------------------------------------------------
SELECT column গুলোতে —
✔ হয় Aggregate Function থাকতে হবে
✔ অথবা GROUP BY তে থাকতে হবে
-----------------------------------------------------------------------------------------

❌ ভুল:
-----------------------------------------------------------------------------------------
SELECT product, amount FROM sales GROUP BY product;
-----------------------------------------------------------------------------------------

✔ সঠিক:
-----------------------------------------------------------------------------------------
SELECT product, SUM(amount) FROM sales GROUP BY product;
-----------------------------------------------------------------------------------------

=========================================================================================
# 14. Execution Order (Query Flow)
=========================================================================================
SQL ভিতরে যেভাবে কাজ করে:

1️⃣ FROM  
2️⃣ WHERE  
3️⃣ GROUP BY  
4️⃣ HAVING  
5️⃣ SELECT  
6️⃣ ORDER BY  
7️⃣ LIMIT  

=========================================================================================
# 15. Real-Life Use Cases
=========================================================================================
✔ Department-wise salary  
✔ Product-wise sales  
✔ Category-wise count  
✔ Monthly revenue report  
✔ Analytics dashboards  

=========================================================================================
# 16. Common Mistakes
=========================================================================================
❌ HAVING এর জায়গায় WHERE ব্যবহার  
❌ Non-grouped column SELECT করা  
❌ ORDER BY ভুল জায়গায় ব্যবহার  
❌ GROUP BY ছাড়া Aggregate function ভুল বোঝা  

=========================================================================================
# 17. Final Takeaway
=========================================================================================
- GROUP BY data কে summarize করে
- Aggregate function ছাড়া GROUP BY অর্থহীন
- WHERE = row filter
- HAVING = group filter
- Reporting & analytics এর backbone

=========================================================================================
                                THE END
=========================================================================================
