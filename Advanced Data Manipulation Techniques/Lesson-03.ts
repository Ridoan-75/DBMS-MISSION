=========================================================================================
                            DISTINCT & WHERE FILTERING 
=========================================================================================

=========================================================================================
# 0. DATA FILTERING কেন দরকার?
=========================================================================================
Database এ সাধারণত —
👉 Duplicate data থাকে
👉 সব data একসাথে দরকার হয় না

তাই দরকার হয় —
- DISTINCT → Duplicate বাদ দিতে
- WHERE → নির্দিষ্ট condition অনুযায়ী data filter করতে

=========================================================================================
=========================================================================================
                            DISTINCT
=========================================================================================
=========================================================================================

=========================================================================================
# 1. DISTINCT কী?
=========================================================================================
DISTINCT ব্যবহার করা হয় —
👉 Duplicate value বাদ দিয়ে unique value দেখানোর জন্য

=========================================================================================
# 2. DISTINCT BASIC SYNTAX
=========================================================================================

SELECT DISTINCT column_name
FROM table_name;

=========================================================================================
# 3. SINGLE COLUMN DISTINCT
=========================================================================================

-- সব unique city দেখাবে
SELECT DISTINCT city
FROM users;

-- Duplicate city একবারই দেখাবে

=========================================================================================
# 4. MULTIPLE COLUMN DISTINCT
=========================================================================================

SELECT DISTINCT city, country
FROM users;

-- Explanation:
-- city + country combination unique হতে হবে

=========================================================================================
# 5. DISTINCT WITH ORDER BY
=========================================================================================

SELECT DISTINCT city
FROM users
ORDER BY city ASC;

=========================================================================================
# 6. DISTINCT WITH WHERE
=========================================================================================

SELECT DISTINCT city
FROM users
WHERE country = 'Bangladesh';

=========================================================================================
# 7. COMMON DISTINCT MISTAKES
=========================================================================================
❌ SELECT name, DISTINCT city  (ভুল)
❌ DISTINCT সব column এ কাজ করে না ভেবে নেওয়া
❌ Large table এ performance issue ignore করা

=========================================================================================
=========================================================================================
                            WHERE FILTERING
=========================================================================================
=========================================================================================

=========================================================================================
# 8. WHERE কী?
=========================================================================================
WHERE ব্যবহার করা হয় —
👉 নির্দিষ্ট condition অনুযায়ী row filter করার জন্য

=========================================================================================
# 9. WHERE BASIC SYNTAX
=========================================================================================

SELECT column1, column2
FROM table_name
WHERE condition;

=========================================================================================
# 10. COMPARISON OPERATORS
=========================================================================================

=   -- Equal  
!=  -- Not equal  
<>  -- Not equal  
>   -- Greater than  
<   -- Less than  
>=  -- Greater or equal  
<=  -- Less or equal  

=========================================================================================
# 11. WHERE WITH NUMBER
=========================================================================================

SELECT name, age
FROM users
WHERE age >= 18;

=========================================================================================
# 12. WHERE WITH STRING
=========================================================================================

SELECT name, city
FROM users
WHERE city = 'Dhaka';

=========================================================================================
# 13. WHERE WITH DATE
=========================================================================================

SELECT name, created_at
FROM users
WHERE created_at >= '2024-01-01';

=========================================================================================
# 14. LOGICAL OPERATORS (AND, OR, NOT)
=========================================================================================

-- AND
SELECT name, age
FROM users
WHERE age >= 18 AND city = 'Dhaka';

-- OR
SELECT name
FROM users
WHERE city = 'Dhaka' OR city = 'Chittagong';

-- NOT
SELECT name
FROM users
WHERE NOT city = 'Dhaka';

=========================================================================================
# 15. WHERE WITH BETWEEN
=========================================================================================

SELECT name, age
FROM users
WHERE age BETWEEN 18 AND 30;

=========================================================================================
# 16. WHERE WITH IN
=========================================================================================

SELECT name, city
FROM users
WHERE city IN ('Dhaka', 'Rajshahi', 'Khulna');

=========================================================================================
# 17. WHERE WITH LIKE (PATTERN MATCHING)
=========================================================================================

-- নাম A দিয়ে শুরু
SELECT name
FROM users
WHERE name LIKE 'A%';

-- নাম a দিয়ে শেষ
SELECT name
FROM users
WHERE name LIKE '%a';

-- নামের মাঝে an আছে
SELECT name
FROM users
WHERE name LIKE '%an%';

=========================================================================================
# 18. WHERE WITH IS NULL / IS NOT NULL
=========================================================================================

SELECT name
FROM users
WHERE phone IS NULL;

SELECT name
FROM users
WHERE phone IS NOT NULL;

=========================================================================================
# 19. DISTINCT + WHERE + ORDER BY (COMBINATION)
=========================================================================================

SELECT DISTINCT city
FROM users
WHERE country = 'Bangladesh'
ORDER BY city ASC;

=========================================================================================
# 20. EXECUTION ORDER (IMPORTANT)
=========================================================================================
SQL internally যেভাবে কাজ করে —

FROM  
→ WHERE  
→ SELECT  
→ DISTINCT  
→ ORDER BY  
→ LIMIT  

=========================================================================================
# 21. REAL-WORLD EXAMPLES
=========================================================================================

-- Unique cities where users are adults
SELECT DISTINCT city
FROM users
WHERE age >= 18;

-- Recent active users
SELECT name, created_at
FROM users
WHERE created_at >= '2025-01-01'
ORDER BY created_at DESC;

=========================================================================================
# 22. COMMON WHERE MISTAKES
=========================================================================================
❌ String value এ single quote না দেওয়া  
❌ NULL check এ = NULL ব্যবহার করা  
❌ AND / OR precedence না বোঝা  
❌ Date format mismatch  

=========================================================================================
# 23. QUICK SUMMARY
=========================================================================================
DISTINCT → Duplicate remove  
WHERE → Row filter  
AND / OR / NOT → Multiple condition  
IN → Multiple value check  
BETWEEN → Range check  
LIKE → Pattern search  
IS NULL → Missing value check  

=========================================================================================
                                   THE END
=========================================================================================
