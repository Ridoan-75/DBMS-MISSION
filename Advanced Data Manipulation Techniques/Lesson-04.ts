=========================================================================================
                                FILTERING WITH AND & OR 
=========================================================================================

=========================================================================================
# 0. AND & OR কেন গুরুত্বপূর্ণ?
=========================================================================================
Database query তে প্রায়ই —
👉 একাধিক condition একসাথে check করতে হয়

এই কাজের জন্য ব্যবহার হয় —
- AND
- OR

=========================================================================================
# 1. AND OPERATOR কী?
=========================================================================================
AND মানে —
👉 সবগুলো condition true হতে হবে

TRUE AND TRUE   → TRUE  
TRUE AND FALSE  → FALSE  
FALSE AND FALSE → FALSE  

=========================================================================================
# 2. AND BASIC SYNTAX
=========================================================================================

SELECT column1, column2
FROM table_name
WHERE condition1 AND condition2;

=========================================================================================
# 3. AND EXAMPLE (NUMBER + STRING)
=========================================================================================

-- বয়স ১৮ বা তার বেশি এবং city Dhaka
SELECT name, age, city
FROM users
WHERE age >= 18 AND city = 'Dhaka';

=========================================================================================
# 4. MULTIPLE AND CONDITIONS
=========================================================================================

SELECT name, age, city
FROM users
WHERE age >= 18
AND city = 'Dhaka'
AND is_active = true;

=========================================================================================
=========================================================================================
                            OR OPERATOR
=========================================================================================
=========================================================================================

=========================================================================================
# 5. OR OPERATOR কী?
=========================================================================================
OR মানে —
👉 যেকোনো একটা condition true হলেই হবে

TRUE OR FALSE  → TRUE  
FALSE OR FALSE → FALSE  

=========================================================================================
# 6. OR BASIC SYNTAX
=========================================================================================

SELECT column1, column2
FROM table_name
WHERE condition1 OR condition2;

=========================================================================================
# 7. OR EXAMPLE
=========================================================================================

-- Dhaka অথবা Chittagong শহরের user
SELECT name, city
FROM users
WHERE city = 'Dhaka' OR city = 'Chittagong';

=========================================================================================
=========================================================================================
                    AND + OR TOGETHER (IMPORTANT)
=========================================================================================
=========================================================================================

=========================================================================================
# 8. AND & OR একসাথে ব্যবহার
=========================================================================================

-- ⚠️ AND এর precedence বেশি
SELECT name, age, city
FROM users
WHERE age >= 18
AND city = 'Dhaka'
OR city = 'Chittagong';

-- Execution:
-- (age >= 18 AND city = 'Dhaka') OR city = 'Chittagong'

=========================================================================================
# 9. PARENTHESES ব্যবহার (BEST PRACTICE)
=========================================================================================

-- বয়স ১৮+ এবং (Dhaka অথবা Chittagong)
SELECT name, age, city
FROM users
WHERE age >= 18
AND (city = 'Dhaka' OR city = 'Chittagong');

=========================================================================================
# 10. WRONG vs RIGHT EXAMPLE
=========================================================================================

-- ❌ ভুল (logic mismatch)
SELECT *
FROM users
WHERE age >= 18
AND city = 'Dhaka'
OR city = 'Chittagong';

-- ✅ সঠিক
SELECT *
FROM users
WHERE age >= 18
AND (city = 'Dhaka' OR city = 'Chittagong');

=========================================================================================
=========================================================================================
                        PRACTICAL PATTERNS
=========================================================================================
=========================================================================================

=========================================================================================
# 11. AND WITH BETWEEN
=========================================================================================

SELECT name, age
FROM users
WHERE age BETWEEN 18 AND 30
AND is_active = true;

=========================================================================================
# 12. OR WITH IN (CLEAN APPROACH)
=========================================================================================

-- OR এর বদলে IN ব্যবহার করা ভালো
SELECT name, city
FROM users
WHERE city IN ('Dhaka', 'Chittagong', 'Rajshahi');

=========================================================================================
# 13. AND WITH LIKE
=========================================================================================

SELECT name
FROM users
WHERE name LIKE 'A%'
AND city = 'Dhaka';

=========================================================================================
# 14. AND WITH IS NULL
=========================================================================================

SELECT name
FROM users
WHERE phone IS NULL
AND is_active = true;

=========================================================================================
=========================================================================================
                        EXECUTION & LOGIC
=========================================================================================
=========================================================================================

=========================================================================================
# 15. OPERATOR PRECEDENCE
=========================================================================================
Order:
1️⃣ NOT  
2️⃣ AND  
3️⃣ OR  

=========================================================================================
# 16. EXECUTION ORDER (SIMPLIFIED)
=========================================================================================
FROM  
→ WHERE (AND / OR apply here)  
→ SELECT  
→ ORDER BY  

=========================================================================================
=========================================================================================
                        COMMON MISTAKES
=========================================================================================
=========================================================================================

❌ Parentheses ব্যবহার না করা  
❌ OR বেশি ব্যবহার করে unwanted row আনা  
❌ AND / OR precedence না বোঝা  
❌ Complex condition এক লাইনে লেখা  

=========================================================================================
# 17. REAL-WORLD EXAMPLES
=========================================================================================

-- Active adult users in Dhaka or Chittagong
SELECT name, age, city
FROM users
WHERE is_active = true
AND age >= 18
AND (city = 'Dhaka' OR city = 'Chittagong');

-- Inactive or underage users
SELECT name, age
FROM users
WHERE is_active = false
OR age < 18;

=========================================================================================
# 18. QUICK SUMMARY
=========================================================================================
AND → সব condition true  
OR → যেকোনো এক condition true  
() → Logic control  
IN → OR এর cleaner version  

=========================================================================================
                                      THE END
=========================================================================================
