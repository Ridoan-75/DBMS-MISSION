=========================================================================================
                SQL NOT OPERATOR & SCALAR FUNCTIONS 
=========================================================================================

=========================================================================================
# 1. NOT Operator কী?
=========================================================================================
NOT হলো—
👉 একটি Logical Operator  
👉 কোনো condition কে উল্টে দেয় (TRUE → FALSE, FALSE → TRUE)

সহজ ভাষায়:
👉 "না" বা "উল্টো শর্ত"

Syntax:
SELECT *
FROM table_name
WHERE NOT condition;

=========================================================================================
# 2. NOT কোথায় ব্যবহার হয়?
=========================================================================================
NOT সাধারণত ব্যবহার হয়—
✔ NOT LIKE
✔ NOT IN
✔ NOT BETWEEN
✔ NOT EXISTS
✔ NOT (condition)

=========================================================================================
# 3. NOT Operator Examples
=========================================================================================

-- ধরো টেবিল: students
-- columns: id, name, age, department, gpa

-----------------------------------------------------------------------------------------
-- 3.1 NOT LIKE
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE name NOT LIKE 'A%';

-----------------------------------------------------------------------------------------
-- 3.2 NOT IN
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE department NOT IN ('CSE', 'EEE');

-----------------------------------------------------------------------------------------
-- 3.3 NOT BETWEEN
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE age NOT BETWEEN 18 AND 25;

-----------------------------------------------------------------------------------------
-- 3.4 NOT with condition
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE NOT (gpa >= 3.50);

-- উপরের query সমান:
SELECT *
FROM students
WHERE gpa < 3.50;

=========================================================================================
# 4. Scalar Function কী?
=========================================================================================
Scalar Function হলো—
👉 এমন function যা
👉 প্রতিটি row এর জন্য
👉 একটি single value return করে

এক লাইনে:
👉 One row → One output value

=========================================================================================
# 5. Scalar Function এর Categories
=========================================================================================

1) String Functions
2) Numeric Functions
3) Date / Time Functions
4) Conversion Functions
5) Conditional Functions

=========================================================================================
# 6. STRING SCALAR FUNCTIONS
=========================================================================================

-----------------------------------------------------------------------------------------
-- 6.1 UPPER() → সব অক্ষর বড় হাতের
-----------------------------------------------------------------------------------------
SELECT UPPER(name) AS upper_name
FROM students;

-----------------------------------------------------------------------------------------
-- 6.2 LOWER() → সব অক্ষর ছোট হাতের
-----------------------------------------------------------------------------------------
SELECT LOWER(name) AS lower_name
FROM students;

-----------------------------------------------------------------------------------------
-- 6.3 LENGTH() → string এর length
-----------------------------------------------------------------------------------------
SELECT name, LENGTH(name) AS name_length
FROM students;

-----------------------------------------------------------------------------------------
-- 6.4 CONCAT() → একাধিক string জোড়া
-----------------------------------------------------------------------------------------
SELECT CONCAT(name, ' - ', department) AS student_info
FROM students;

-----------------------------------------------------------------------------------------
-- 6.5 SUBSTRING()
-----------------------------------------------------------------------------------------
SELECT SUBSTRING(name FROM 1 FOR 3) AS short_name
FROM students;

=========================================================================================
# 7. NUMERIC SCALAR FUNCTIONS
=========================================================================================

-----------------------------------------------------------------------------------------
-- 7.1 ABS() → absolute value
-----------------------------------------------------------------------------------------
SELECT ABS(-10);

-----------------------------------------------------------------------------------------
-- 7.2 ROUND()
-----------------------------------------------------------------------------------------
SELECT ROUND(gpa, 2)
FROM students;

-----------------------------------------------------------------------------------------
-- 7.3 CEILING() / FLOOR()
-----------------------------------------------------------------------------------------
SELECT CEILING(3.2), FLOOR(3.8);

=========================================================================================
# 8. DATE / TIME SCALAR FUNCTIONS
=========================================================================================

-----------------------------------------------------------------------------------------
-- 8.1 CURRENT_DATE
-----------------------------------------------------------------------------------------
SELECT CURRENT_DATE;

-----------------------------------------------------------------------------------------
-- 8.2 CURRENT_TIMESTAMP
-----------------------------------------------------------------------------------------
SELECT CURRENT_TIMESTAMP;

-----------------------------------------------------------------------------------------
-- 8.3 AGE()
-----------------------------------------------------------------------------------------
SELECT AGE(CURRENT_DATE, birth_date)
FROM students;

-----------------------------------------------------------------------------------------
-- 8.4 EXTRACT()
-----------------------------------------------------------------------------------------
SELECT EXTRACT(YEAR FROM CURRENT_DATE);

=========================================================================================
# 9. CONVERSION SCALAR FUNCTIONS
=========================================================================================

-----------------------------------------------------------------------------------------
-- 9.1 CAST()
-----------------------------------------------------------------------------------------
SELECT CAST(gpa AS INTEGER)
FROM students;

-----------------------------------------------------------------------------------------
-- 9.2 :: Operator (PostgreSQL shortcut)
-----------------------------------------------------------------------------------------
SELECT gpa::INTEGER
FROM students;

=========================================================================================
# 10. CONDITIONAL SCALAR FUNCTIONS
=========================================================================================

-----------------------------------------------------------------------------------------
-- 10.1 COALESCE()
-----------------------------------------------------------------------------------------
SELECT COALESCE(phone, 'N/A')
FROM students;

-----------------------------------------------------------------------------------------
-- 10.2 NULLIF()
-----------------------------------------------------------------------------------------
SELECT NULLIF(gpa, 0)
FROM students;

=========================================================================================
# 11. NOT with Scalar Functions
=========================================================================================

-----------------------------------------------------------------------------------------
-- 11.1 NOT with LIKE + LOWER
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE LOWER(name) NOT LIKE '%rahim%';

-----------------------------------------------------------------------------------------
-- 11.2 NOT with COALESCE
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE NOT (COALESCE(gpa, 0) >= 3.00);

=========================================================================================
# 12. REAL-WORLD USE CASES
=========================================================================================

-----------------------------------------------------------------------------------------
-- 12.1 Invalid Email Filter
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE email NOT LIKE '%@%';

-----------------------------------------------------------------------------------------
-- 12.2 Students with Missing Phone
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE phone IS NULL;

-- Equivalent using NOT:
SELECT *
FROM students
WHERE NOT (phone IS NOT NULL);

-----------------------------------------------------------------------------------------
-- 12.3 Low GPA Report
-----------------------------------------------------------------------------------------
SELECT name, gpa
FROM students
WHERE NOT (gpa >= 3.50);

=========================================================================================
# 13. COMMON MISTAKES
=========================================================================================
1) NOT আর NULL behavior ভুল বোঝা  
2) NOT IN ব্যবহার করা যখন list এ NULL আছে  
3) Complex condition এ bracket না দেওয়া  
4) Scalar function WHERE clause এ বেশি ব্যবহার করা (index issue)

=========================================================================================
# 14. BEST PRACTICES
=========================================================================================
1) Simple condition হলে NOT ছাড়া direct logic লেখো  
2) Readability এর জন্য bracket ব্যবহার করো  
3) Performance critical জায়গায় function-aware index ব্যবহার করো  
4) Scalar function SELECT এ বেশি ব্যবহার করো, WHERE এ কম  

=========================================================================================
# 15. FINAL SUMMARY
=========================================================================================
✔ NOT → condition উল্টায়  
✔ Scalar Function → row-wise single value  
✔ String, Numeric, Date, Conversion সব খুব গুরুত্বপূর্ণ  
✔ PostgreSQL real-world production এ ব্যাপকভাবে ব্যবহৃত  

=========================================================================================
                                    THE END
=========================================================================================
