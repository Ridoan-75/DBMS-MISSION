=========================================================================================
                    SQL LIKE vs ILIKE (PostgreSQL)
=========================================================================================

=========================================================================================
# 1. LIKE কী?
=========================================================================================
LIKE ব্যবহার করা হয়—
👉 text / string pattern match করার জন্য  
👉 case-sensitive comparison করার জন্য (PostgreSQL এ)

LIKE মানে:
👉 “এই pattern এর সাথে মিল আছে কিনা?”

Syntax:
SELECT *
FROM table_name
WHERE column_name LIKE 'pattern';

=========================================================================================
# 2. LIKE = Case-Sensitive (PostgreSQL)
=========================================================================================
PostgreSQL এ—
👉 LIKE case-sensitive  
👉 বড় হাতের অক্ষর (A) আর ছোট হাতের অক্ষর (a) আলাদা ধরা হয়  

=========================================================================================
# 3. LIKE এর Wildcards
=========================================================================================

Wildcard | অর্থ
---------|----------------------------------
%        | যেকোনো সংখ্যা character
_        | ঠিক একটি character

=========================================================================================
# 4. LIKE Examples
=========================================================================================

-- ধরো একটি টেবিল: users
-- columns: id, name, email

-----------------------------------------------------------------------------------------
-- 4.1 নাম "A" দিয়ে শুরু
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name LIKE 'A%';

-- A, Alex, Ahmed → match করবে
-- aaron → match করবে না (case-sensitive)

-----------------------------------------------------------------------------------------
-- 4.2 নাম "n" দিয়ে শেষ
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name LIKE '%n';

-----------------------------------------------------------------------------------------
-- 4.3 মাঝখানে "ha" আছে
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name LIKE '%ha%';

-----------------------------------------------------------------------------------------
-- 4.4 ঠিক 4 অক্ষরের নাম
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name LIKE '____';

=========================================================================================
# 5. ILIKE কী?
=========================================================================================
ILIKE হলো—
👉 PostgreSQL–specific operator  
👉 case-insensitive version of LIKE  

ILIKE মানে:
👉 বড় হাতের / ছোট হাতের অক্ষর যাই হোক—ম্যাচ করবে

Syntax:
SELECT *
FROM table_name
WHERE column_name ILIKE 'pattern';

=========================================================================================
# 6. ILIKE = Case-Insensitive
=========================================================================================
PostgreSQL এ—
👉 ILIKE case-insensitive  
👉 'A' == 'a'  
👉 'HELLO' == 'hello'

=========================================================================================
# 7. ILIKE Examples
=========================================================================================

-----------------------------------------------------------------------------------------
-- 7.1 নাম "a" দিয়ে শুরু (case-insensitive)
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name ILIKE 'a%';

-- A, a, Alex, ahmed → সব match করবে

-----------------------------------------------------------------------------------------
-- 7.2 Email এ "gmail" আছে কিনা
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE email ILIKE '%gmail%';

-----------------------------------------------------------------------------------------
-- 7.3 নাম "N" দিয়ে শেষ (case-insensitive)
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name ILIKE '%n';

=========================================================================================
# 8. LIKE vs ILIKE (Side-by-Side Comparison)
=========================================================================================

Feature              | LIKE                         | ILIKE
---------------------|------------------------------|------------------------------
Case Sensitive       | হ্যাঁ (PostgreSQL)           | না
PostgreSQL Specific  | না                           | হ্যাঁ
Use Case             | Exact text match             | User-friendly search
Performance          | একটু faster                  | একটু slower
Common Usage         | System-level data            | Search / filter UI

=========================================================================================
# 9. LIKE / ILIKE with NOT
=========================================================================================

-----------------------------------------------------------------------------------------
-- 9.1 NOT LIKE
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name NOT LIKE 'A%';

-----------------------------------------------------------------------------------------
-- 9.2 NOT ILIKE
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE email NOT ILIKE '%gmail%';

=========================================================================================
# 10. REAL-WORLD USE CASES
=========================================================================================

-----------------------------------------------------------------------------------------
-- 10.1 Search feature (recommended)
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE name ILIKE '%rahim%';

-----------------------------------------------------------------------------------------
-- 10.2 Admin filtering (strict)
-----------------------------------------------------------------------------------------
SELECT *
FROM users
WHERE role LIKE 'ADMIN%';

-----------------------------------------------------------------------------------------
-- 10.3 Product Search
-----------------------------------------------------------------------------------------
SELECT *
FROM products
WHERE product_name ILIKE '%iphone%';

=========================================================================================
# 11. COMMON MISTAKES
=========================================================================================
1) LIKE দিয়ে case-insensitive search আশা করা  
2) % wildcard ব্যবহার না করা  
3) PostgreSQL ছাড়া অন্য DB তে ILIKE ব্যবহার করা  
4) Large table এ index ছাড়া ILIKE ব্যবহার করা  

=========================================================================================
# 12. PERFORMANCE NOTE (IMPORTANT)
=========================================================================================
ILIKE ব্যবহার করলে—
👉 Normal B-Tree index কাজ নাও করতে পারে  

Solution:
👉 Lowercase column + functional index

Example:
CREATE INDEX idx_users_name_lower
ON users (LOWER(name));

Then:
SELECT *
FROM users
WHERE LOWER(name) LIKE '%rahim%';

=========================================================================================
# 13. BEST PRACTICES
=========================================================================================
1) User search → ILIKE  
2) Strict matching → LIKE  
3) Large dataset → proper indexing  
4) Clean pattern ব্যবহার করো  

=========================================================================================
# 14. FINAL SUMMARY
=========================================================================================
✔ LIKE → case-sensitive pattern match  
✔ ILIKE → case-insensitive pattern match (PostgreSQL)  
✔ % → any length  
✔ _ → single character  
✔ Search feature এ ILIKE সবচেয়ে বেশি ব্যবহৃত  

=========================================================================================
                                    THE END
=========================================================================================
