=========================================================================================
                SQL COMPARISON OPERATORS, BETWEEN & IN 
=========================================================================================

=========================================================================================
# 0. এই অধ্যায়ে আমরা কী শিখবো?
=========================================================================================
এই অধ্যায়ে আমরা শিখবো—
1) SQL Comparison Operators (তুলনামূলক অপারেটর)
2) BETWEEN কী এবং কীভাবে কাজ করে
3) IN কী এবং কীভাবে কাজ করে
4) বাস্তব উদাহরণ (PostgreSQL ভিত্তিক)
5) Common Mistakes + Best Practices

=========================================================================================
# 1. COMPARISON OPERATORS কী?
=========================================================================================
Comparison Operator ব্যবহার করে আমরা—
👉 দুইটি মান (value) তুলনা করি  
👉 WHERE clause এর মাধ্যমে নির্দিষ্ট শর্তে ডাটা ফিল্টার করি  

=========================================================================================
# 2. COMPARISON OPERATORS এর তালিকা
=========================================================================================

Operator     | অর্থ
-------------|------------------------------------
=            | সমান
!= অথবা <>   | সমান নয়
>            | বড়
<            | ছোট
>=           | বড় অথবা সমান
<=           | ছোট অথবা সমান

=========================================================================================
# 3. COMPARISON OPERATOR ব্যবহার উদাহরণ
=========================================================================================

-- ধরো একটি টেবিল আছে: students
-- columns: id, name, age, gpa, department

-----------------------------------------------------------------------------------------
-- 3.1 Equal (=)
-----------------------------------------------------------------------------------------
SELECT * 
FROM students
WHERE department = 'CSE';

-----------------------------------------------------------------------------------------
-- 3.2 Not Equal (!= অথবা <>)
-----------------------------------------------------------------------------------------
SELECT * 
FROM students
WHERE department != 'EEE';

-----------------------------------------------------------------------------------------
-- 3.3 Greater Than (>)
-----------------------------------------------------------------------------------------
SELECT * 
FROM students
WHERE age > 20;

-----------------------------------------------------------------------------------------
-- 3.4 Less Than (<)
-----------------------------------------------------------------------------------------
SELECT * 
FROM students
WHERE gpa < 3.00;

-----------------------------------------------------------------------------------------
-- 3.5 Greater Than or Equal (>=)
-----------------------------------------------------------------------------------------
SELECT * 
FROM students
WHERE gpa >= 3.50;

-----------------------------------------------------------------------------------------
-- 3.6 Less Than or Equal (<=)
-----------------------------------------------------------------------------------------
SELECT * 
FROM students
WHERE age <= 22;

=========================================================================================
# 4. BETWEEN কী?
=========================================================================================
BETWEEN ব্যবহার করা হয়—
👉 একটি নির্দিষ্ট range (শুরু থেকে শেষ) এর ভেতরের ডাটা বের করতে  

গুরুত্বপূর্ণ বিষয়:
👉 BETWEEN inclusive  
👉 মানে শুরু এবং শেষ—দুটোই অন্তর্ভুক্ত থাকে  

Syntax:
SELECT *
FROM table_name
WHERE column_name BETWEEN value1 AND value2;

=========================================================================================
# 5. BETWEEN এর উদাহরণ
=========================================================================================

-----------------------------------------------------------------------------------------
-- 5.1 Age 18 থেকে 25 এর মধ্যে
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE age BETWEEN 18 AND 25;

-----------------------------------------------------------------------------------------
-- এইটা সমান:
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE age >= 18 AND age <= 25;

-----------------------------------------------------------------------------------------
-- 5.2 GPA Range
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE gpa BETWEEN 3.00 AND 4.00;

-----------------------------------------------------------------------------------------
-- 5.3 Date Range (PostgreSQL)
-----------------------------------------------------------------------------------------
SELECT *
FROM orders
WHERE order_date BETWEEN '2025-01-01' AND '2025-12-31';

=========================================================================================
# 6. BETWEEN ব্যবহার করার সময় Common Mistake
=========================================================================================

-- ভুল ধারণা:
-- BETWEEN exclusive না, inclusive

-- ভুল:
-- ভাবা যে 18 আর 25 বাদ যাবে

-- সত্য:
-- 18 এবং 25 দুইটাই থাকবে

=========================================================================================
# 7. IN কী?
=========================================================================================
IN ব্যবহার করা হয়—
👉 একাধিক নির্দিষ্ট মানের মধ্যে যেকোনো একটির সাথে match করাতে  

IN মানে:
👉 OR এর শর্টকাট ভার্সন  

Syntax:
SELECT *
FROM table_name
WHERE column_name IN (value1, value2, value3);

=========================================================================================
# 8. IN এর উদাহরণ
=========================================================================================

-----------------------------------------------------------------------------------------
-- 8.1 Multiple Departments
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE department IN ('CSE', 'EEE', 'BBA');

-----------------------------------------------------------------------------------------
-- এইটা সমান:
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE department = 'CSE'
   OR department = 'EEE'
   OR department = 'BBA';

-----------------------------------------------------------------------------------------
-- 8.2 Specific Ages
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE age IN (18, 20, 22);

-----------------------------------------------------------------------------------------
-- 8.3 ID List
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE id IN (1, 3, 5, 7);

=========================================================================================
# 9. NOT IN
=========================================================================================
NOT IN ব্যবহার করা হয়—
👉 নির্দিষ্ট মানগুলো বাদ দিতে  

-----------------------------------------------------------------------------------------
-- 9.1 특정 Department বাদ
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE department NOT IN ('CSE', 'EEE');

=========================================================================================
# 10. BETWEEN vs IN (Comparison)
=========================================================================================

Feature        | BETWEEN                    | IN
---------------|----------------------------|------------------------------
কাজ            | Range ভিত্তিক              | নির্দিষ্ট মানের তালিকা
Use Case       | বয়স, তারিখ, salary         | category, id, status
Inclusive?     | হ্যাঁ                      | Exact match
Alternative    | >= AND <=                  | OR condition

=========================================================================================
# 11. REAL-WORLD QUERY EXAMPLES
=========================================================================================

-----------------------------------------------------------------------------------------
-- 11.1 বয়স 18–30 এবং Department নির্দিষ্ট
-----------------------------------------------------------------------------------------
SELECT *
FROM students
WHERE age BETWEEN 18 AND 30
  AND department IN ('CSE', 'EEE');

-----------------------------------------------------------------------------------------
-- 11.2 GPA High Performers
-----------------------------------------------------------------------------------------
SELECT name, gpa
FROM students
WHERE gpa >= 3.75;

-----------------------------------------------------------------------------------------
-- 11.3 নির্দিষ্ট তারিখের অর্ডার
-----------------------------------------------------------------------------------------
SELECT *
FROM orders
WHERE order_date BETWEEN '2025-06-01' AND '2025-06-30'
  AND status IN ('completed', 'shipped');

=========================================================================================
# 12. BEST PRACTICES
=========================================================================================
1) Range check এর জন্য BETWEEN ব্যবহার করো  
2) Multiple OR থাকলে IN ব্যবহার করো  
3) Date filtering এ BETWEEN খুবই শক্তিশালী  
4) Clean ও readable query লেখো  

=========================================================================================
# 13. FINAL SUMMARY
=========================================================================================
✔ Comparison Operators → শর্ত অনুযায়ী তুলনা  
✔ BETWEEN → range ভিত্তিক filtering  
✔ IN → multiple exact values check  
✔ PostgreSQL এ সবগুলো daily production এ ব্যবহার হয়  

=========================================================================================
                NEXT TOPIC READY WHEN YOU ARE 🔥
=========================================================================================
