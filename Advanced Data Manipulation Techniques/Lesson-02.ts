=========================================================================================
                SELECT BASICS: SORTING (ORDER BY) & ALIASES
=========================================================================================

=========================================================================================
# 0. SELECT QUERY কেন গুরুত্বপূর্ণ?
=========================================================================================
SELECT ব্যবহার করা হয় —
👉 Database থেকে data পড়ার (retrieve) জন্য

এই chapter এ আমরা শিখবো —
- Sorting (ORDER BY)
- Aliases (AS)
- Real-world examples
- Common mistakes

=========================================================================================
# 1. SORTING কী? (ORDER BY)
=========================================================================================
Sorting মানে —
👉 Data কে নির্দিষ্ট order এ সাজানো

দুই ধরনের sorting হয় —
1️⃣ ASC  → Ascending (ছোট → বড় / A → Z)
2️⃣ DESC → Descending (বড় → ছোট / Z → A)

=========================================================================================
# 2. ORDER BY এর BASIC SYNTAX
=========================================================================================

SELECT column_name
FROM table_name
ORDER BY column_name ASC|DESC;

=========================================================================================
# 3. ASCENDING SORT (DEFAULT)
=========================================================================================

-- বয়স ছোট থেকে বড়
SELECT name, age
FROM users
ORDER BY age ASC;

-- NOTE:
-- ASC না লিখলেও default ভাবে ASC ধরা হয়

SELECT name, age
FROM users
ORDER BY age;

=========================================================================================
# 4. DESCENDING SORT
=========================================================================================

-- বয়স বড় থেকে ছোট
SELECT name, age
FROM users
ORDER BY age DESC;

=========================================================================================
# 5. STRING SORTING (TEXT)
=========================================================================================

-- নাম alphabetically (A → Z)
SELECT name
FROM users
ORDER BY name ASC;

-- নাম reverse order (Z → A)
SELECT name
FROM users
ORDER BY name DESC;

=========================================================================================
# 6. DATE SORTING
=========================================================================================

-- পুরনো date আগে
SELECT name, created_at
FROM users
ORDER BY created_at ASC;

-- নতুন date আগে
SELECT name, created_at
FROM users
ORDER BY created_at DESC;

=========================================================================================
# 7. MULTIPLE COLUMN SORTING
=========================================================================================
প্রথম column একই হলে → দ্বিতীয় column apply হয়

SELECT name, age, city
FROM users
ORDER BY city ASC, age DESC;

-- Explanation:
-- আগে city অনুযায়ী sort
-- একই city হলে age বড় থেকে ছোট

=========================================================================================
=========================================================================================
                            ALIASES (AS)
=========================================================================================
=========================================================================================

=========================================================================================
# 8. ALIAS কী?
=========================================================================================
Alias মানে —
👉 Column বা table এর temporary nickname

Alias ব্যবহার করা হয় —
- Output readable করার জন্য
- Column নাম ছোট করার জন্য
- Calculated column এর জন্য

=========================================================================================
# 9. COLUMN ALIAS (AS)
=========================================================================================

SELECT name AS user_name, age AS user_age
FROM users;

-- AS optional
SELECT name user_name, age user_age
FROM users;

=========================================================================================
# 10. CALCULATED COLUMN WITH ALIAS
=========================================================================================

SELECT name, salary * 12 AS yearly_salary
FROM employees;

-- Explanation:
-- salary * 12 এর result কে yearly_salary নামে দেখাবে

=========================================================================================
# 11. ALIAS WITH STRING
=========================================================================================

SELECT name AS "User Name", age AS "User Age"
FROM users;

-- NOTE:
-- Space থাকলে double quotes ব্যবহার করতে হবে

=========================================================================================
# 12. TABLE ALIAS
=========================================================================================

SELECT u.name, u.email
FROM users AS u;

-- AS optional
SELECT u.name, u.email
FROM users u;

=========================================================================================
# 13. TABLE ALIAS কেন দরকার?
=========================================================================================
✔ Query ছোট হয়  
✔ JOIN এর সময় confusion কমে  
✔ Professional SQL লেখার standard  

=========================================================================================
# 14. ORDER BY WITH ALIAS
=========================================================================================

SELECT name, salary * 12 AS yearly_salary
FROM employees
ORDER BY yearly_salary DESC;

-- NOTE:
-- ORDER BY এ alias ব্যবহার করা যায়

=========================================================================================
# 15. ORDER BY COLUMN POSITION
=========================================================================================

SELECT name, age, salary
FROM employees
ORDER BY 2 DESC;

-- Explanation:
-- 2 মানে second column → age

⚠️ Best practice না, readable না

=========================================================================================
# 16. REAL-WORLD EXAMPLES
=========================================================================================

-- Top 5 highest paid employees
SELECT name, salary
FROM employees
ORDER BY salary DESC
LIMIT 5;

-- Latest users first
SELECT name, created_at
FROM users
ORDER BY created_at DESC;

=========================================================================================
# 17. COMMON MISTAKES
=========================================================================================
❌ ORDER BY আগে WHERE লেখা  
❌ DESC / ASC ভুল জায়গায় লেখা  
❌ Alias এ space দিয়ে double quote না দেওয়া  
❌ Column position ব্যবহার করা production এ  

=========================================================================================
# 18. EXECUTION ORDER (IMPORTANT)
=========================================================================================
SQL internally যেভাবে execute হয় —

FROM  
→ WHERE  
→ SELECT  
→ ORDER BY  
→ LIMIT  

=========================================================================================
# 19. QUICK SUMMARY
=========================================================================================
ORDER BY → Sorting  
ASC → Small to large  
DESC → Large to small  
AS → Alias / nickname  
Alias → Readability + professionalism  

=========================================================================================
                                    END
=========================================================================================
